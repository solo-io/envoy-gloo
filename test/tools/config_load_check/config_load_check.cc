// NOLINT(namespace-envoy)
#include <iostream>
#include <string>

// This file only exists to get upstream's `do_ci.sh` to pass, as it is
// hard-coded to build this target

int main(int, char**) {
  return EXIT_SUCCESS;
}
